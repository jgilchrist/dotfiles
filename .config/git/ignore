# This file serves as a system-wide .gitignore and as such should contain
# files which should be ignored on all systems.
#
# It shouldn't contain ignore patterns for files specific to any single
# repository. For example, ignore patterns for specific auxiliary IDE files
# should be placed in the repository's .git/info/exclude file.

# MacOS
.DS_Store

# Ignore files
.ignore

# env files
.env

# IDE files
.idea/
*.DotSettings.user
