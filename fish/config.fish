if [ "$TERM" != "screen-256color" ]
    set TERM xterm-256color
end

set LD_LIBRARY_PATH /usr/local/lib $LD_LIBRARY_PATH

alias b "cd .."

alias :x "exit"
alias :q "exit"

alias toilet "toilet -f smblock"
